import { StaticImageData } from "next/image";

export interface IProjects {
    name:string,
    subTitle:string,
    description:string,
    live:string,
    repo:string,
    image:StaticImageData,
    status:string,
    tools:string
}


